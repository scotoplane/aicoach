import { HomeConfig } from 'types';

const homeConfig: HomeConfig = {
  mainNav: [
    {
      title: 'About',
      href: '/#about',
    },
    {
      title: 'Contact',
      href: '/contact',
    },
  ],
};

export default homeConfig;
